Multi-agent content creation app from an empty folder. 
Using LangChain's createAgent — no LangGraph graph API needed. 
Give it a topic, and a supervisor agent orchestrates five specialist agents to research, write, illustrate, fact-check, and promote a full article. 
Every artifact lands as a markdown file (plus one image) — no database required.
